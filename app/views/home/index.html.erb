<%= render partial: 'home/partials/header', locals: { title: 'Diving into marine metadata' } %>

<% themes.each do |theme| %>
  <%= render partial: '/home/partials/theme', locals: { theme: theme } %>
<% end %>