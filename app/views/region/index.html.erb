<%= render partial: 'global/header', locals: { title: t('region.title'), intro: t('region.intro') } %>

<div class="container">
  <div class="padding--page">
    <metadata
      data-src="/region-list"
      post="/region-download"
      :table-headers="<%= @table_headers %>"
      :items-per-page="10">
    </metadata>
  </div>
</div>

<%= render 'region/logos' %>

<div class="container">
  <div class="smallprint section-padding">
    <p><strong>Disclaimers</strong></p>
    <p>
      <%= t('region.disclaimer-cpps') %>
    </p>
  </div>
</div>
